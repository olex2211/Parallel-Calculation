# ════════════════════════════════════════════
#  PostgreSQL for Billing Service
# ════════════════════════════════════════════
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres-billing
  labels:
    app: postgres-billing
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres-billing
  template:
    metadata:
      labels:
        app: postgres-billing
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              value: "clinic_user"
            - name: POSTGRES_PASSWORD
              value: "clinic_password"
            - name: POSTGRES_DB
              value: "clinic_billing"
          volumeMounts:
            - name: billing-data
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - clinic_user
                - -d
                - clinic_billing
            initialDelaySeconds: 5
            periodSeconds: 5
      volumes:
        - name: billing-data
          persistentVolumeClaim:
            claimName: billing-data-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-billing
spec:
  selector:
    app: postgres-billing
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: billing-data-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 256Mi
